<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<h1>All Collections</h1>
<ul><% @brands.each do |collect| %>
<li><%= link_to collect.owner.name, owner_path(collect.owner) %>'s <%= link_to collect.category, brand_path(collect) %></li>
<% end %></ul>


<%= link_to "New Collection", new_brand_path %>