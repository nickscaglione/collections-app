<h3>Create a new Collection</h3>

<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= form_for(@brand) do |f| %>

  <p><%= f.text_field :category, placeholder: "brand category" %></p>

  <%= f.submit %>

<% end %>
