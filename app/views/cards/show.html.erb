<%= link_to @card.collection.owner.name, owner_path(@card.collection.owner) %>'s
<%= @card.count %>x
<%= @card.name %>
from the <%= link_to @card.collection.category, collection_path(@card.collection) %> collection.
