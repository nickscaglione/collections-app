<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>


<h1><%= link_to @owner.name, owner_path(@owner) %>'s decks</h1>

<ul><% @my_decks.each do |deck| %>
  <li><%= link_to deck.name, deck_path(deck) %></li>
    <ul> <% deck.cards_with_count.each do |array| %>
<li>        <%= array[0] %>x <%= link_to array[1].name, card_path(array[1]) %> </li> 
        <% end %></ul>
<% end %>
</ul>
